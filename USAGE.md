# 燕雲兌換助手 v0.5.1

解壓縮到可寫入的資料夾，開啟「燕雲兌換助手.exe」。若遊戲以系統管理員執行，請右鍵助手並選「以系統管理員身分執行」。不需要 Codex、Python、API 金鑰或 Discord Token。

適用 Windows 10/11 64 位元、Discord 桌面版繁中介面、Steam 繁中版《燕雲十六聲》，需有 Windows 英文及繁體中文 OCR 語言功能。

**建議將遊戲解析度設為 1920 × 1080，並使用視窗化或無邊框模式。** 目前畫面辨識及操作位置以此介面實測，其他解析度尚未完整驗證。

## 四個操作

- 掃描頻道：首次掃描全部文字歷史；之後只補上次完整進度以後的訊息。以訊息 ID 接續，同一天稍晚新增的訊息也不會漏掉。掃描不會自動開始兌換。
- 快速兌換：直接使用已掃描紀錄，處理按下按鈕時往回 15 天內訊息中的待兌換碼，不重新掃描。
- 完整兌換：直接使用已掃描紀錄，處理全部待兌換碼，不重新掃描。
- 停止：保存進度後停止；F10 也可隨時停止。

沒有本批上限。兩種兌換模式都會持續處理符合範圍的待兌換碼，直到完成、手動停止或遇到無法確認的情況。已成功、已領取、已過期、無效及已達上限的碼會跳過；待確認的碼不會自動重送。

## 使用方式

1. 輸入遊戲左下角的角色 ID，按 Enter 或點其他位置即可載入該角色進度。
2. 在自己的 Discord 帳號開啟有閱讀權限的兌換碼討論串，複製該討論串連結。
3. 首次或有新訊息時，按「掃描頻道」。首次會請你貼上來源連結，只保存在本機。舊版會從既有紀錄沿用來源；已有掃描紀錄可直接兌換。
4. 建議遊戲設為 1920 × 1080，使用視窗化或無邊框模式，停在「設定 → 其他 → 兌換碼」。按「快速兌換」或「完整兌換」。

執行期間會控制滑鼠鍵盤及覆蓋剪貼簿，請暫停其他操作。程式確認角色、輸入文字及遊戲回應後逐筆保存。單碼「已達上限」會記錄後繼續；每日／帳號限制或不明回應會停止。

## 進度與補掃

每次掃描先到最新訊息，往回與上次完整掃描位置接合；相鄰頁必須有重疊。中斷或漏頁時不推進完成位置，下次重新補齊。若舊進度訊息已被刪除，會往回核對至討論串起點。

只讀文字，不辨識 Discord 圖片或追蹤外部連結。增量掃描只更新本次讀到的訊息，不會重新檢查更早歷史中的編輯。

右鍵點選結果清單可查看截圖、來源，以及手動確認結果或重新嘗試。請依實際遊戲回應處理待確認項目。

## 更新與分享

進度保存在執行檔旁 data 資料夾，依角色 ID 分開。更新前關閉助手，替換程式並保留 data；換電腦時搬移整個資料夾。

給朋友請傳「燕雲兌換助手.zip」。ZIP 只有 EXE 和這份說明，不含你的帳號、Discord 文字、進度或截圖。

公開版沒有預設討論串。朋友需自行設定有權限閱讀的來源；來源連結保存在該角色 data/profiles/<角色 ID>/source.txt，與進度一起留在本機。

最近錯誤在 data/last-error.txt；檢查與結果畫面在 data/profiles/<角色 ID>/。不要直接分享整個 data 資料夾。
