Read `../../AGENTS.md` if it exists in the local workspace. Keep credentials, source URLs, personal redemption data, and screenshots local and untracked. Use synthetic fixtures in tests. Release archives must contain only the executable and public instructions. Never mark a redemption successful without an observed game response. UI automation must stop on ambiguity, focus loss, or cancellation.
